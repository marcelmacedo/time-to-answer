<h1>Site::Welcome#index</h1>
<p>Find me in app/views/site/welcome/index.html.erb</p>

<% if user_signed_in? %>
  <p>Usuário <b><%= current_user.email %></b> ESTÁ logado.</p>
  <% else %>
  <p>Usuário NÃO está logado.</p>
<% end %>


<% if admin_signed_in? %>
  <p>Administrador <b> <%= current_admin.email %> </b> ESTÁ logado.</p>
  
  <% else %>
  <p>Administrador NÃO está logado.</p>
<% end %>